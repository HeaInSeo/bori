apiVersion: v1
kind: Service
metadata:
  name: bori-operator-metrics
  namespace: bori-system
  labels:
    app.kubernetes.io/name: bori-operator
    app.kubernetes.io/component: controller
spec:
  selector:
    app.kubernetes.io/name: bori-operator
  ports:
    - name: metrics
      port: 8080
      targetPort: 8080
      protocol: TCP

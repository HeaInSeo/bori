apiVersion: apps/v1
kind: Deployment
metadata:
  name: nodesentinel
  namespace: nodesentinel-system
  labels:
    app.kubernetes.io/name: nodesentinel
    app.kubernetes.io/component: validation-agent
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: nodesentinel
  template:
    metadata:
      labels:
        app.kubernetes.io/name: nodesentinel
        app.kubernetes.io/component: validation-agent
    spec:
      serviceAccountName: nodesentinel
      securityContext:
        runAsNonRoot: true
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: nodesentinel
          image: ghcr.io/heainseo/node-sentinel:v0.1.0
          imagePullPolicy: IfNotPresent
          ports:
            - name: grpc
              containerPort: 50052
              protocol: TCP
            - name: http
              containerPort: 8080
              protocol: TCP
          env:
            - name: NODESENTINEL_GRPC_PORT
              value: "50052"
            - name: NODESENTINEL_HTTP_PORT
              value: "8080"
            - name: NODESENTINEL_DB_PATH
              value: /var/lib/nodesentinel/nodesentinel.db
            - name: NODEVAULT_API_ADDR
              value: http://nodevault-controlplane.nodevault-system.svc:8082
            - name: SMOKE_NAMESPACE
              value: nodevault-smoke
          volumeMounts:
            - name: data
              mountPath: /var/lib/nodesentinel
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: "1"
              memory: 512Mi
          readinessProbe:
            httpGet:
              path: /readyz
              port: http
          livenessProbe:
            httpGet:
              path: /healthz
              port: http
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop:
                - ALL
      volumes:
        - name: data
          emptyDir: {}

apiVersion: v1
kind: Service
metadata:
  name: node-sentinel
  namespace: node-sentinel-system
spec:
  selector:
    app: node-sentinel
  ports:
    - name: metrics
      port: 8080
      targetPort: 8080
    - name: health
      port: 8081
      targetPort: 8081

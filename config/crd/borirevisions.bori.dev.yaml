---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.21.0
  name: borirevisions.bori.dev
spec:
  group: bori.dev
  names:
    kind: BoriRevision
    listKind: BoriRevisionList
    plural: borirevisions
    shortNames:
    - brev
    singular: borirevision
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.release
      name: Release
      type: string
    - jsonPath: .spec.environment
      name: Environment
      type: string
    - jsonPath: .status.promotionStatus
      name: Status
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        properties:
          apiVersion:
            type: string
          kind:
            type: string
          metadata:
            type: object
          spec:
            properties:
              baselineRef:
                type: string
              components:
                items:
                  properties:
                    componentSpecDigest:
                      type: string
                    environmentDigest:
                      type: string
                    gitSha:
                      type: string
                    imageDigest:
                      type: string
                    imageRef:
                      type: string
                    name:
                      type: string
                    verificationPolicyDigest:
                      type: string
                    version:
                      type: string
                  required:
                  - name
                  - version
                  type: object
                type: array
              contentHash:
                type: string
              environment:
                type: string
              parentRevisionId:
                type: string
              release:
                type: string
            required:
            - components
            - contentHash
            - environment
            - release
            type: object
          status:
            properties:
              observedAt:
                format: date-time
                type: string
              promotedAt:
                format: date-time
                type: string
              promotionStatus:
                type: string
              verificationRunId:
                type: string
            required:
            - observedAt
            - promotionStatus
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}

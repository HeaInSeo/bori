linters:
  enable:
    - govet
    - staticcheck
    - ineffassign
    - unused
    - errcheck
    - revive

linters-settings:
  revive:
    rules:
      - name: exported
        disabled: true

run:
  timeout: 5m

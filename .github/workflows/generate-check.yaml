name: generate-check

on:
  push:
    branches: [main]
    paths:
      - "apis/**"
      - "config/crd/**"
      - "config/rbac/**"
      - "Makefile"
      - "go.mod"
      - "go.sum"
      - "tools/tools.go"
      - ".github/workflows/generate-check.yaml"
  pull_request:
    branches: [main]
    paths:
      - "apis/**"
      - "config/crd/**"
      - "config/rbac/**"
      - "Makefile"
      - "go.mod"
      - "go.sum"
      - "tools/tools.go"
      - ".github/workflows/generate-check.yaml"

permissions:
  contents: read

jobs:
  check:
    name: Verify CRD + DeepCopy generated files are up-to-date
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - uses: actions/setup-go@v7
        with:
          go-version-file: go.mod
          cache: true

      - name: Run make generate
        run: make generate

      - name: Check for diff
        run: |
          if ! git diff --exit-code config/crd/ apis/bori/v1alpha1/zz_generated.deepcopy.go; then
            echo "ERROR: Generated files are out of date."
            echo "Run 'make generate' and commit the result."
            exit 1
          fi
